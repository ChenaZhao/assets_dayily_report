# 公开仓库(GitHub Pages)只发布 reports/html/ 的成品 HTML。
# 以下私有内容一律不上传(源码/数据/提示词/Excel/中间产物)，即便误用 git add -A 也不会泄露。
/src/
/data/
/prompts/
/assets/
/.claude/
/__pycache__/
*.xlsx
*.bak
config.py
run_data.py
CLAUDE.md
工作流说明.md

# reports 仅本地: md 原稿/旧 html/归档都不上传(发布走 asset/macro)
/reports/

# 公开发布目录: asset/macro(本宏观条线)。asset/equity、asset/futures 为别的系统, 本脚本不动。
# 这些不在忽略列表中, 即正常跟踪发布。
